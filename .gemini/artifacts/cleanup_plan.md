# Codebase Cleanup Plan

## Objective

Remove all unnecessary notes, comments, and lint-related files from the codebase to make it cleaner and more professional.

## Items to Remove

### 1. Lint Output Files (7 files)

These are temporary files generated during linting and should be deleted:

- `lint-output.json` (776 KB)
- `lint.txt` (72 KB)
- `lint_errors.txt` (empty)
- `lint_final.txt` (51 KB)
- `lint_output.txt` (52 KB)
- `lint_report.json` (1.3 MB)
- `lint_v2.txt` (53 KB)

**Total space to free: ~2.3 MB**

### 2. ESLint Disable Comments (8 instances)

Remove or fix the underlying issues for these eslint-disable comments:

#### Files with eslint-disable comments:

1. `utils/action-helpers.ts` (line 70)
2. `tailwind.config.ts` (line 40)
3. `components/NotificationBell.tsx` (line 69)
4. `components/sidebar/SidebarGroup.tsx` (line 23)
5. `app/dashboard/cases/[id]/CaseDetailsClient.tsx` (line 161)
6. `app/dashboard/cases/[id]/actions.ts` (line 737)
7. `app/api/documents/download/route.ts` (line 14)
8. `app/api/documents/download/route.ts` (line 106)

### 3. Other Unnecessary Comments

- `proxy.ts` (line 13): Informational note about edge environment
- `components/CaseTimeline.tsx` (line 41): Comment "// Notes"

**Note:** `next-env.d.ts` should NOT be edited as it's auto-generated by Next.js

## Approach

### Phase 1: Delete Lint Files ✓

Simply delete all 7 lint output files.

### Phase 2: Review ESLint Disable Comments

For each eslint-disable comment:

1. Check if the underlying code can be fixed properly
2. If fixable, fix the code and remove the comment
3. If necessary (e.g., legitimate use of `any` type), keep the comment but add explanation

### Phase 3: Clean Up Minor Comments

Remove trivial comments that don't add value.

## Expected Outcome

- Cleaner codebase without temporary lint files
- Properly typed code without eslint-disable workarounds where possible
- More professional and maintainable code
